# Recursive Binary Search

def binary_search(data, target) -> bool:
    """Returns true if target is contained in data"""
    return True

assert binary_search([2, 8, 12, 18], 12) == True, '12 is supposed to be present'